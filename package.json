{
  "name": "subscription-manager-root",
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:frontend && npm run build:frontend"
  },
  "dependencies": {
    "axios": "^1.16.1"
  }
}
